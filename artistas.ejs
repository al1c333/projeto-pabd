<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <title>Listagem de Artistas</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel='stylesheet' href='/stylesheets/style.css' />
</head>
<body class="bg-light">
    <div class="container mt-4">
        <h1 class="text-center fw-bold mb-4">Listagem de Artistas</h1>
        <a href="http://localhost:3000/artistas/add" class="btn btn-success" style="margin-bottom: 10px;">Incluir</a>
        <div class="table-responsive">
            <table class="table table-striped table-hover shadow-sm">
    <thead class="table-primary">
        <tr>
            <th>#</th>
            <th>Título</th>
            <th>Ano de Lançamento</th>
            <th>Artista</th>
        </tr>
    </thead>
    <tbody>
        <% for (item of resultado) { %>
        <tr>
            <td><%= item.id_album %></td>
            <td><%= item.titulo %></td>
            <td><%= item.ano_lancamento %></td>
            <td><%= item.artista %></td>
        </tr>
        <% } %>
    </tbody>
</table>
        </div>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>